// ====================== HSK_DB ======================
const HSK_DB = {
    hsk1: ["我","你","他","她","是","的","人","不","在","有","这","中","会","为","上","大","个","地","来","出","时","国","和","也","子","到","说","要","下","好","吗","呢","老师","学生","爸爸","妈妈","中国","美国","日本","韩国","英国","去","吃","喝","买","喜欢","我们","你们","他们","她们","这里","哪里"],
    hsk2: ["吧","白","百","报","过","还","近","进","就","觉得","看","考","可以","块","乐","离","两","路","卖","慢","忙","每","妹妹","门","面","名","饭","面","饺子","冰淇淋","水","茶","可乐","咖啡","酒","米饭","汤","热","粥","豆腐","对","律师","中文","英文","韩文","课","音乐","数学","大学生","老公","老婆","常常","不常","书","书店","超市","饭馆","菜","绿茶","零食","谢谢","杯","牛奶","冰","不客气","手机","钱包","洗手间","火车","火车站","哎呀","行李","车票","站台","同学","新","今天","你好","四","历史","真的吗","叫","我叫","运动","篮球","打","跑步","非常","游泳","网球","还是","比较","公园","健身房"],
    hsk3: ["阿","啊","矮","安静","把","搬","办法","办公室","半","帮忙","包","饱","方","准备","差","最後","虽然","经常","愿意","饿","累","小","大人","小人","巴西","巴西人","中国人"],
    hsk4: ["爱情","安排","安全","暗示","按照","百分之","棒","抱歉","保证","报名","抱","倍","本来","笨","比如","毕业","并且","标准","表达","表现","表演","博物馆","餐厅","参加","曾经","产品","成功","成绩","诚实","充满","穿过","传统","刺激","聪明","错误","答案","当地","当时","道理","登机","等待"],
    hsk5: ["调查","导演","得意","敌人","地球","地震","电梯","电子","对方","对手","动物","独特","度过","锻炼","对面","多亏","朵","恶劣","耳环","发表","法律","反应","范围","方法","方向","放弃","放假","放松","飞机","废话","分布","分手","风景","符合","复杂","负责","附近","改变","改善","干杯","感动","感谢","刚好","高级","根据","更加","工资","公平","沟通","购物","够","估计","鼓励","故意","关键","管理","光","广告","规定","规则","国际","果然","过程","害羞","寒假","行业","合格","合适","合作","盒子","厚","后悔","呼吸","忽然","护士","怀疑","环境","活动","火","机会","基础","及时","激烈","计划","继续","加油","家庭","假设","价格","坚决","建议","将来","讲究","交流","教授","教育","结果","解决","借","紧张","进入","禁止","究竟","决定","绝对","开放","看法","考虑","考试","可怜","可能","客人","空调","空闲","控制","困难","扩大","困","拉","来不及","浪费","老板","浪漫","老虎","老实","乐观","雷","理解","理论","理想","厉害","力量","立刻","联系","练习","良好","聊天","临时","另外","流行","留","录取","旅行","麻烦","马上","满意","毛巾","帽子","美丽","梦想","迷路","密码","免费","面对","面积","秒","明白","明确","明显","命令","目的","耐心","难道","能力","年纪","年轻","努力","暖和","偶然","怕","判断","陪","配合","佩服","碰","批评","脾气","篇","骗","骗子","拼","平常","平时","评论","葡萄","普遍","期待","奇怪","奇迹","起来","气氛","千","千万","签证","墙","抢","强烈","桥","亲切","亲戚","情况","请求","球迷","取得","取消","确定","确实","裙子","然而","然后","染","热闹","忍","认识","认为","任务","仍然","日历","容易","如果","软件","散步","沙发","伤心","商量","社会","射","摄影","深","甚至","失败","失望","失去","时候","实验","实在","似的","收藏","收据","收拾","手表","手术","首","primeiro","recebido","magro","confortável","tio","familiar","quantidade","número","escovar","cair","nível","talvez","persuadir","pensamento","parecer","velocidade","plástico","calcular","casual","sempre","neto","encurtar","portanto","todos","mesa","senhora","conversar","namorar","sopa","doce","especial","característica","dor","levantar","fornecer","tópico","clima","esporte","ingênuo","desafio","condição","dançar","notificar","através","compaixão","concordar","cabelo","repentino","terra","coelho","união","empurrar","tirar","boneca","concluir","perfeito","dez mil","rede","site","esquecer","perigo","ameaça","sorriso","único","sabor","temperatura","quente","arquivo","cultura","artigo","cheirar","estável","entediado","independentemente","mal-entendido","atrair","lavar","banho","sistema","drama","baixar","verão","exibir","moderno","realidade","fenômeno","agora","limite","perfume","detalhado","imaginar","elefante","mensagem","lanche","rapaz","cuidado","rir","efeito","eficiência","piada","escrita","difícil","apreciar","carta","informação","animado","feliz","personalidade","reparar","demanda","necessidade","propaganda","escolher","semestre","treinamento","pressão","dente","pesquisa","estrito","grave","óculos","sol","criar","aparência","exigir","convidar","chave","página","amador","geral","metade","certo","total","um pouco","alguns","sempre","móvel","imigrante","depois","antes","pensar","opinião","significado","acidente","sombra","música","banco","bebida","impressão","deveria","influência","ganhar","duro","sempre","usar","vantagem","excelente","humor","especialmente","por","devido a","correio","jogo","amigável","amizade","peixe","alegre","chuva","encontrar","previsão","reservar","original","razão","marcar","ler","lua","permitir","revista","adeus","temporário","terrível","cedo","causar","responsabilidade","como","como","aumentar","ocupar","parar","folha","longo","dominar","cuidar","câmera","apressado","filosofia","verdadeiro","lutar","normal","organizado","provar","apoio","conhecimento","valer a pena","direto","planta","profissão","apenas","apontar","qualidade","pelo menos","papel","meio","intermediário","foco","importante","semana","ao redor","porco","bambu","ideia","desejar","famoso","atenção","especializado","profissional","virar","ganhar","preparar","preciso","pontual","mesa","incêndio","capital","cuidadoso","filho","roxo","caractere","desde","automático","natural","self-service","sempre","total","suficiente","futebol","formar","ancestral","respeito","ontem","recente","obedecer","esquerda direita","escritor","autor","fazer","cozinhar","método","fazer negócios","lição de casa"]
};

function getHSK(front) {
    const order = ['hsk5', 'hsk4', 'hsk3', 'hsk2', 'hsk1'];
    for (let lvl of order) {
        if (HSK_DB[lvl].some(w => front.includes(w))) return lvl;
    }
    return '';
}

// ====================== Variáveis globais ======================
const STORAGE_KEY = 'mandarim_ebbinghaus_final';
const INTERVALS = [0, 10, 1440, 2880, 7200, 21600, 43200, 86400];
let decks = JSON.parse(localStorage.getItem(STORAGE_KEY)) || [];
let curSession = { deckIdx: 0, cards: [], cur: 0, mode: '', subMode: '', answered: false };
let voices = [];

// ====================== Inicialização ======================
window.addEventListener('load', () => { 
    setTimeout(() => { 
        document.getElementById('splash').style.opacity = '0'; 
        setTimeout(() => document.getElementById('splash').style.display = 'none', 300); 
    }, 300); 
    updateStreak();
    renderDecks();
    loadVoices();
});

// ====================== Funções básicas ======================
function showSection(id) { 
    document.querySelectorAll('.section').forEach(s => s.classList.remove('active')); 
    document.getElementById(id).classList.add('active'); 
    if(id === 'library') renderDecks(); 
}

function save() { 
    localStorage.setItem(STORAGE_KEY, JSON.stringify(decks)); 
}

// ====================== Renderização ======================
function renderDecks() {
    const container = document.getElementById('deck-list'); 
    container.innerHTML = '';
    let tA = 0, tR = 0, tM = 0; 
    const now = Date.now();
    let hskM = { hsk1: 0, hsk2: 0, hsk3: 0, hsk4: 0, hsk5: 0 };

    decks.forEach((d, i) => {
        const rev = d.cards.filter(c => (c.level || 0) < 6 && (c.nextReview || 0) <= now).length;
        const mas = d.cards.filter(c => (c.level || 0) >= 6).length;
        const perc = d.cards.length ? Math.round((mas / d.cards.length) * 100) : 0;

        d.cards.forEach(c => { 
            if (c.level >= 6) { 
                const lvl = getHSK(c.front); 
                if (lvl) hskM[lvl]++; 
            } 
        });

        tA += d.cards.length; 
        tR += rev; 
        tM += mas;
        
        container.innerHTML += `
            <div class="deck-card">
                <button onclick="deleteDeck(${i}, event)" style="position:absolute;top:10px;right:10px;border:none;background:none;color:#666;">✖</button>
                <strong class="deck-title">${d.name}</strong>
                <div class="prog-bg"><div class="prog-fill" style="width:${perc}%"></div></div>
                <div style="display:grid; grid-template-columns: 1fr 1fr; gap:5px;">
                    <button class="main-btn" style="font-size:0.6em" onclick="startStudy(${i},'typing','visual')">⌨ LER</button>
                    <button class="main-btn" style="font-size:0.6em;background:#8e44ad" onclick="startStudy(${i},'choice','visual')">🔘 LER</button>
                    <button class="main-btn" style="font-size:0.6em;background:#2c3e50" onclick="startStudy(${i},'typing','audio')">🎧 OUVIR</button>
                    <button class="main-btn" style="font-size:0.6em;background:#d35400" onclick="startStudy(${i},'choice','audio')">🔊 OUVIR</button>
                </div>
            </div>`;
    });

    document.getElementById('dash-total').innerText = tA;
    document.getElementById('dash-review').innerText = tR;
    document.getElementById('dash-mastery').innerText = tA ? Math.round((tM/tA)*100)+"%" : "0%";
    renderHSK(hskM);
}

function renderHSK(data) {
    const hskContainer = document.getElementById('hsk-bars'); 
    hskContainer.innerHTML = '';
    ['hsk1','hsk2','hsk3','hsk4','hsk5'].forEach(lvl => {
        const total = HSK_DB[lvl].length;
        const p = total ? Math.min(Math.round((data[lvl] / total) * 100), 100) : 0;
        hskContainer.innerHTML += `
            <div class="hsk-bar-container">
                <div class="hsk-meta"><span>${lvl.toUpperCase()}</span><span>${p}%</span></div>
                <div class="prog-bg" style="margin:0;height:6px;">
                    <div class="prog-fill ${lvl}" style="width:${p}%"></div>
                </div>
            </div>`;
    });
}

function updateStreak() {
    const stats = JSON.parse(localStorage.getItem('mandarim_stats')) || { lastDate: '', streak: 0 };
    const today = new Date().toLocaleDateString();
    const yesterday = new Date(Date.now() - 86400000).toLocaleDateString();
    if (stats.lastDate === yesterday) stats.streak++;
    else if (stats.lastDate !== today) stats.streak = 1;
    stats.lastDate = today;
    localStorage.setItem('mandarim_stats', JSON.stringify(stats));
    document.getElementById('dash-streak').innerText = stats.streak;
}

// ====================== SYNC - JSONCrush ======================
function generateSyncCode() {
    if (!decks || decks.length === 0) return alert("Nada para exportar.");

    const now = Date.now();
    const mini = decks.map(d => ({
        n: d.name,
        c: d.cards.map(card => [
            card.front,
            card.back,
            card.level || 0,
            card.nextReview ? Math.round((card.nextReview - now) / 3600000) : 0
        ])
    }));

    const jsonStr = JSON.stringify(mini);
    
    let code = typeof JSONCrush !== "undefined" 
        ? JSONCrush.crush(jsonStr) 
        : LZString.compressToEncodedURIComponent(jsonStr);

    navigator.clipboard.writeText(code).then(() => {
        alert(`✅ Código gerado!\n\nTamanho: ${code.length} caracteres`);
    }).catch(() => alert("Erro ao copiar. Tamanho: " + code.length));
}

function importViaCode() {
    const code = prompt("Cole o código de sync:");
    if (!code || !code.trim()) return;

    try {
        let jsonStr = typeof JSONCrush !== "undefined" 
            ? JSONCrush.uncrush(code.trim()) 
            : LZString.decompressFromEncodedURIComponent(code.trim());

        const data = JSON.parse(jsonStr);
        const now = Date.now();

        decks = data.map(item => ({
            name: item.n,
            cards: item.c.map(arr => ({
                front: arr[0],
                back: arr[1],
                level: arr[2],
                nextReview: now + (arr[3] * 3600000)
            }))
        }));

        save();
        renderDecks();
        alert(`✅ Importado com sucesso! (${decks.length} baralhos)`);
    } catch (e) {
        alert("❌ Código inválido.");
        console.error(e);
    }
}

// ====================== Estudo ======================
function startStudy(i, mode, subMode) {
    const due = decks[i].cards.filter(c => (c.nextReview || 0) <= Date.now() || (c.level || 0) === 0);
    if(!due.length) return alert("Nada para revisar!");
    
    curSession = { 
        deckIdx: i, 
        mode, 
        subMode, 
        cards: due.sort(() => Math.random() - 0.5).slice(0, 15), 
        cur: 0, 
        answered: false 
    };
    showSection('study-area'); 
    renderCard();
}

function renderCard() {
    const c = curSession.cards[curSession.cur];
    const display = document.getElementById('display-term');

    if (curSession.subMode === 'audio') {
        display.innerText = "🔊";
        speak(c.front);
    } else {
        display.innerText = c.back.includes('-') ? c.back.split('-')[1].trim() : c.back;
    }
    
    document.getElementById('prog-text').innerText = `${curSession.cur + 1} / ${curSession.cards.length}`;
    document.getElementById('feedback').innerText = ''; 
    document.getElementById('next-btn').style.display = 'none';
    curSession.answered = false;

    const zone = document.getElementById('interaction-zone');
    if (curSession.mode === 'typing') {
        zone.innerHTML = `<input type="text" id="type-in" placeholder="Digite o Hanzi..." autocomplete="off">`;
        const input = document.getElementById('type-in');
        input.addEventListener('keypress', (e) => { if(e.key === 'Enter') handleTyping(e); });
        setTimeout(() => input.focus(), 150);
    } else {
        const opts = [c.front, ...decks[curSession.deckIdx].cards.map(x => x.front)
            .filter(f => f !== c.front)
            .sort(() => 0.5 - Math.random())
            .slice(0, 3)]
            .sort(() => 0.5 - Math.random());

        zone.innerHTML = `<div class="options-grid">${opts.map(o => 
            `<button class="opt-btn" onclick="checkChoice(this,'${o.replace(/'/g, "\\'")}')">${o}</button>`
        ).join('')}</div>`;
    }
}

function checkChoice(btn, val) {
    if (curSession.answered) return;
    if (val === curSession.cards[curSession.cur].front) {
        btn.classList.add('correct');
        handleResult(true);
    } else {
        btn.classList.add('wrong');
        handleResult(false);
    }
}

function handleTyping(e) {
    if (!curSession.answered) handleResult(e.target.value.trim() === curSession.cards[curSession.cur].front);
}

function handleResult(isCorrect) {
    curSession.answered = true;
    const c = curSession.cards[curSession.cur];
    if (isCorrect) c.level = (c.level || 0) + 1;
    else c.level = Math.max((c.level || 0) - 2, 0);
    c.nextReview = Date.now() + (INTERVALS[Math.min(c.level, 7)] * 60000);
    
    const fb = document.getElementById('feedback');
    fb.innerText = isCorrect ? "🔥 Excelente!" : "🧊 Revisar: " + c.front;
    fb.style.color = isCorrect ? "var(--success)" : "var(--danger)";
    
    save();
    document.getElementById('next-btn').style.display = 'block';
    speak(c.front);
}

function processNext() { 
    curSession.cur++; 
    if (curSession.cur < curSession.cards.length) renderCard(); 
    else showSection('library'); 
}

// ====================== Outras funções ======================
function saveDeck() {
    const n = document.getElementById('deck-name').value.trim();
    const t = document.getElementById('bulk-input').value.trim();
    if (n && t) {
        decks.push({ 
            name: n, 
            cards: t.split('\n').map(l => {
                const [f, b] = l.split(';');
                return f && b ? { front: f.trim(), back: b.trim(), level: 0, nextReview: 0 } : null;
            }).filter(c => c) 
        });
        save(); 
        showSection('library');
    }
}

function deleteDeck(i, e) { 
    e.stopPropagation(); 
    if (confirm("Apagar este baralho?")) { 
        decks.splice(i, 1); 
        save(); 
        renderDecks(); 
    } 
}

function loadVoices() { 
    voices = window.speechSynthesis.getVoices(); 
    const select = document.getElementById('voice-select');
    select.innerHTML = voices
        .filter(v => v.lang.includes('zh'))
        .map(v => `<option value="${v.name}">${v.name}</option>`)
        .join(''); 
}

function speak(t) { 
    window.speechSynthesis.cancel(); 
    const m = new SpeechSynthesisUtterance(t); 
    m.voice = voices.find(v => v.name === document.getElementById('voice-select').value); 
    m.lang = 'zh-CN'; 
    window.speechSynthesis.speak(m); 
}

function speakCurrent() { 
    if (curSession.cards && curSession.cards[curSession.cur]) 
        speak(curSession.cards[curSession.cur].front); 
}

function exportJSON() { 
    const d = "data:text/json;charset=utf-8," + encodeURIComponent(JSON.stringify(decks)); 
    const a = document.createElement('a'); 
    a.href = d; 
    a.download = "mandarim_backup.json"; 
    a.click(); 
}

function importJSON(e) { 
    const r = new FileReader(); 
    r.onload = (ev) => { 
        decks = JSON.parse(ev.target.result); 
        save(); 
        renderDecks(); 
    }; 
    r.readAsText(e.target.files[0]); 
}

async function requestNotificationPermission() { 
    if (await Notification.requestPermission() === 'granted') alert("Notificações ativadas!"); 
}

document.addEventListener('keydown', (e) => { 
    if (e.key === 'Enter' && document.getElementById('next-btn').style.display === 'block') 
        processNext(); 
});